<mdialog:CustomDialog x:Class="StoryCanvas.WPF.View.SubViews.AuthBrowserSubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
            xmlns:mdialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:StoryCanvas.WPF.View.SubViews"
            xmlns:p="clr-namespace:StoryCanvas.WPF.Properties"
            mc:Ignorable="d" FontSize="14" FontFamily="Meiryo UI"
             d:DesignHeight="300" d:DesignWidth="300" Title="{x:Static p:Resources.Auth}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <WebBrowser x:Name="Browser"/>
        <Button Content="{x:Static p:Resources.Abort}" x:Name="CloseButton" Grid.Row="1" HorizontalAlignment="Right" Width="100" Style="{StaticResource AccentedSquareButtonStyle}"/>
    </Grid>
</mdialog:CustomDialog>
