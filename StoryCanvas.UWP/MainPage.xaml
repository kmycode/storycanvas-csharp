<Page
    x:Class="StoryCanvas.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoryCanvas.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SplitView x:Name="Splitter" DisplayMode="CompactOverlay"
               CompactPaneLength="48" OpenPaneLength="320"
               PaneBackground="Black"
               IsPaneOpen="{Binding ElementName=HamburgerButton,
                            Path=IsChecked, Mode=TwoWay}">
        <SplitView.Pane>
            <Grid RequestedTheme="Dark">
                <StackPanel Margin="0">
                    <ToggleButton x:Name="HamburgerButton" VerticalAlignment="Top" 
                                  Foreground="White"
                                  Background="#3b8940"
                                  Width="48" Height="48">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE700;" />
                    </ToggleButton>
                    <ToggleButton x:Name="RadioButton1" Content="Nav"/>
                    <ToggleButton x:Name="RadioButton2" Content="Nav"/>
                    <ToggleButton x:Name="RadioButton3" Content="Nav"/>
                </StackPanel>
            </Grid>
        </SplitView.Pane>

        <SplitView.Content>
            <Frame x:Name="MainContentFrame" />
        </SplitView.Content>
    </SplitView>
</Page>
